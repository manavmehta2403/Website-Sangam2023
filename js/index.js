// add the aeroplane scroll
//join links in navbar
console.log("connected");
//dynamically set the height of the progress bar
var progressBar = document.querySelector(".progress-container");
var BenefitsSection = document.querySelector(".benefits");

console.log(BenefitsSection.style);

progressBar.style.height =  BenefitsSection.style.height;